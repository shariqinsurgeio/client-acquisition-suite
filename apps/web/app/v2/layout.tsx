import { GrandmasterShell } from "@/components/grandmaster/shell";

export default function V2Layout({ children }: { children: React.ReactNode }) {
  return <GrandmasterShell>{children}</GrandmasterShell>;
}
