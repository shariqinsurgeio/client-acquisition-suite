{
  "name": "agency-os-monorepo",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:web": "npm run dev --workspace=apps/web",
    "dev:ext": "npm run dev --workspace=apps/extension",
    "build": "npm run build --workspaces"
  }
}
